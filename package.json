{
  "name": "codelab-ux",
  "version": "0.0.0",
  "description": "Codelab for Firebase UX",
  "main": ".",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "fmt": "prettier --write scripts/* styles/*"
  },
  "author": "",
  "devDependencies": {
    "prettier": "^1.5.3"
  }
}
